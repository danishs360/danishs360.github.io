# Public URL of the deployed Cloudflare Worker that powers the chatbot.
# In dev, you can point this at `wrangler dev` (default http://localhost:8787).
# In prod, point it at https://portfolio-chat.<your-subdomain>.workers.dev
PUBLIC_CHAT_API_URL=
